version: "3"
services:
  nextjs-app:
    build: ./front-root/docker
    working_dir: /usr/src/app/front-root
    volumes:
      - ./front-root/:/usr/src/app/front-root
    # command: sh -c "npm run dev"
    ports:
      - "3000:3000"
    tty: true